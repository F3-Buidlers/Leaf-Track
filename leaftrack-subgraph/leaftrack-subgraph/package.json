{
  "name": "leaf",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ leaf",
    "create-local": "graph create --node http://localhost:8020/ leaf",
    "remove-local": "graph remove --node http://localhost:8020/ leaf",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 leaf",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.40.0",
    "@graphprotocol/graph-ts": "^0.29.3"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
